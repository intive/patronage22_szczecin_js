const size = {
  s: '576px',
  md: '768px',
  lg: '992px',
  xl: '1440px'
}

export const breakpoint = {
  s: `(min-width: ${size.s})`,
  md: `(min-width: ${size.md})`,
  lg: `(min-width: ${size.lg})`,
  xl: `(min-width: ${size.xl})`
}
